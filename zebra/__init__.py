"""Zebra — monitor any codebase for vulnerabilities, flaws and optimisations.

A zero-dependency-by-default auditor that layers on heavier tools
(semgrep, bandit, pip-audit, trivy, gitleaks ...) when they are installed.
"""

__version__ = "0.1.0"
